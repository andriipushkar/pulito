# «Pack» (pick & pack workflow) — ТОП-5

| #   | Severity | Що                                                                                                                                                                                                                                                                 |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PK1 | 🟠 HIGH  | **Bug:** `/api/v1/admin/orders/packable` route mapping ДРОПАЄ `stockOnHand`, `locationCode`, `locationName` що service обчислює. UI має stock guard (`item.stockOnHand < item.quantity`) і location-sort — обидві фічі мовчать тому що поля не доходять до клієнта |
| PK2 | 🟡 MED   | Pack page робить разовий fetch без polling. Новий confirmed order не з'являється поки оператор не оновить сторінку — упускає робочий потік                                                                                                                         |
| PK3 | 🟡 MED   | Mark-shipped без confirm dialog (irreversible). «Передано курʼєру» → status='shipped', товар уходить з reserved → quantity, payment ledger update. Один випадковий клік = непоправно                                                                               |
| PK4 | 🟡 MED   | `apiClient.put('/status')` із pack не передає `version` — два оператори на одному замовленні → second-write silently wins                                                                                                                                          |
| PK5 | 🟢 LOW   | `forcePick` (override stock check) без logAudit. Якщо оператор force-pick'ом упакував товар якого нема, потім скандал — нема сліду                                                                                                                                 |
